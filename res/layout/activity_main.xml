<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#DAE6F2"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/gameTypeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_centerHorizontal="true"
        android:text="@string/game_type"
        android:textSize="24sp" />

    <Spinner
        android:id="@+id/gameTypeSpinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/gameTypeText"
        android:layout_marginTop="5dp" />

    <NumberPicker
        android:id="@+id/wordLengthNumberPicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/start"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="70dp" />

    <Button
        android:id="@+id/start"
        android:layout_width="90sp"
        android:layout_height="50sp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="26dp"
        android:layout_toRightOf="@+id/wordLengthNumberPicker"
        android:onClick="onClickStart"
        android:text="@string/start"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/wordLengthText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/wordLengthNumberPicker"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5dp"
        android:text="@string/enter_word_length"
        android:textSize="24sp" />

</RelativeLayout>